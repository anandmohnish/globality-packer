{
  "description": "Docker enabled with AWS ECS agent - based on Ubuntu {{user `ubuntu_version`}}",
  "_comment": "Builds an EBS-based AMI.",
  "builders": [
    {
      "ami_name": "{{user `name`}}-ECS-Docker-host-{{user `ubuntu_version`}}-{{user `create_time`}}",
      "ami_description": "{{user `name`}}-ECS-Docker-host, based on Ubuntu {{user `ubuntu_version`}} with AMI {{user `source_ami`}}, created on {{user `create_time`}}",
      "run_tags": { "ami-create": "{{user `name`}}.{{user `aws_region`}}" },
      "source_ami_filter": {
        "filters": {
          "virtualization-type": "hvm",
          "name": "*ubuntu-xenial-16.04-amd64-server-*",
          "root-device-type": "ebs"
        },
        "owners": ["099720109477"],
        "most_recent": true
      },
      "tags": { "ami": "{{user `name`}}.{{user `aws_region`}}" },
